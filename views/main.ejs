<html>
    <head>
        <!-- 합쳐지고 최소화된 최신 CSS -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
        <!-- 합쳐지고 최소화된 최신 자바스크립트 -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
    </head>
    <body>
         <div class="pull-right">
             <a href="/logout">
                <button type="button" class="btn btn-danger">로그아웃</button>
            </a>
         </div>
        <div class="page-header">
            <h1>My Board <small>with Node.js</small></h1>
        </div>

        <div>
            <table class="table table-hover">
                <thead>
                   <tr>
                       <th>No</th>
                       <th>Title</th>
                       <th>작성자</th>
                       <th>조회수</th>
                   </tr>
                </thead>
                <tbody>
                   <% for(var i=0; i<maxDisplayNum; i++){%>
                   <tr>
                        <td><%= boardResult[i].num %></td>
                        <td><%= boardResult[i].title %></td>
                        <td><%= boardResult[i].writer %></td>
                        <td><%= boardResult[i].count %></td>
                    </tr>
                    <%}%>
                </tbody>  
            </table>
            <div>
                <% for(var i=0; i<page; i++){%>
                    <a href=""><%= i+1 %></a>
                <%}%>
            </div>
        </div>
        <a class="btn btn-default" href="/main/new" role="button">글쓰기</a>
    </body>
</html>